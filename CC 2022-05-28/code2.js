/*Now you have to write a function that takes an argument and returns the square of it.*/

//SAMPLE TESTS
/*describe("Tests", () => {
  it("test", () => {
Test.assertEquals(square(3), 9);
});
});*/

//P:Can the function contain strings, arrays, numbers. Can it be positive or negative int
//R:

//SOLUTION:
function square(num){
  //P:Create a function that takes in one parameter
  //square that parameter 
  //Math.pow()
  //Show work
  return Math.pow(num,2)
}


//E:
square(3), 9
square(4), 16